# 一、二叉搜索树（Binary Search Tree）（BST）

## 1.1 定义
一颗有序的有根二叉树，满足：  
1）每个节点存储一个互异的键值（Key）  
2）任一节点左子树的键值<该节点键值，右子树的键值>该节点键值  

## 1.2 构建
1）第一个插入元素为根节点  
2）后续元素从根开始比较，若小于当前节点则往左走，大于则往右走，直到找到空位作为叶子插入  

## 1.3 查找（Find）和插入（Insert）
最坏情况：退化为链，时间复杂度Θ(n)  

平衡BST：高度约为⌈log_2 (n+1)⌉，查找或插入最多比较⌈log_2 (n+1)⌉次，时间复杂度Θ(log n)  

# 二、决策树（Decision Tree）

## 2.1 定义
